# Codex

```bash
codex mcp add omnidim --url https://mcp.omnidim.io/mcp
```

Codex starts the OAuth flow. Then run `codex mcp list` and ask: `What
OmniDimension agents do I have?`

See the [full Codex guide](https://docs.omnidim.io/docs/mcp/connect/codex).
