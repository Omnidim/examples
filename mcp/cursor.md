# Cursor

Add this entry to `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "omnidim": {
      "url": "https://mcp.omnidim.io/mcp"
    }
  }
}
```

Restart Cursor, then ask: `What OmniDimension agents do I have?`

See the [full Cursor guide](https://docs.omnidim.io/docs/mcp/connect/cursor).
